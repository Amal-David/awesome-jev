name: Refresh Jev directory

on:
  schedule:
    - cron: '23 */4 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - 'tests/**'
      - 'data/curated.json'
      - 'data/x_demos.json'
      - 'data/media.json'
      - 'templates/**'
      - 'examples/**'
      - '.github/workflows/curate.yml'

permissions:
  contents: read

concurrency:
  group: awesome-jev-curation
  cancel-in-progress: false

jobs:
  curate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - name: Run offline tests
        run: python3 -m unittest discover -s tests -v
      - name: Render catalog for editorial changes
        if: github.event_name == 'push'
        run: python3 scripts/curate.py
      - name: Discover, verify metadata, and render
        if: github.event_name != 'push'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PYTHONUNBUFFERED: '1'
        run: python3 scripts/curate.py --refresh
      - name: Check public media and render the visual gallery
        run: python3 scripts/curate_media.py --refresh
      - name: Render editorial homepage and curated X demos
        run: python3 scripts/curate_x.py
      - name: Validate generated catalog and editorial pages
        run: |
          python3 scripts/curate.py --check
          python3 scripts/curate_media.py --check
          python3 scripts/curate_x.py --check
      - name: Commit and verify directory updates
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: bash scripts/publish.sh
